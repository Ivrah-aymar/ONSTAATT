/*SELECT J.JOB_ID,E.LAST_NAME
FROM HR.EMPLOYEES E
INNER JOIN HR.JOB_HISTORY J ON
E.JOB_ID=J.JOB_ID;*/
/*SELECT E.*, 
    STATS.MAX_SALARY,
    STATS.MIN_SALARY,
    STATS.AVG_SALARY
    FROM 
    HR.EMPLOYEES E
CROSS JOIN (SELECT MAX(SALARY) AS MAX_SALARY,MIN(SALARY) AS MIN_SALARY,AVG(SALARY) AS AVG_SALARY 
FROM HR.EMPLOYEES
) STATS;*/
/*SELECT MAX(SALARY),MIN(SALARY) FROM HR.EMPLOYEES;*/
/*SELECT AVG(SALARY) AS AVERAGE FROM HR.EMPLOYEES;*/
/*SELECT SUM(SALARY) AS SUMTOTAL FROM HR.EMPLOYEES;*/
/*SELECT DISTINCT * FROM HR.EMPLOYEES;*/
/*Select *
  from (select *
          from HR.EMPLOYEES
         order by dbms_random.value) -- you can also use DBMS_RANDOM.RANDOM
 where rownum < 21;*/
/*for random 5 rows*/
/*FETCH NEXT 10 ROWS ONLY;*/
/*OFFSET 10 ROWS FETCH NEXT 10 ROWS ONLY;*/
/*FETCH  FIRST 10 ROWS ONLY;*/
/*SELECT EMPLOYEE_ID,FIRST_NAME,LAST_NAME,SALARY 
FROM HR.EMPLOYEES
WHERE SALARY > 15000 
ORDER BY LAST_NAME ASC;*/
/*SELECT EMPLOYEE_ID,FIRST_NAME,LAST_NAME,SALARY 
FROM HR.EMPLOYEES
WHERE SALARY > 5000;*/
/*SELECT COUNTRY_ID,COUNTRY_NAME FROM HR.COUNTRIES;*/
/*SELECT * FROM HR.COUNTRIES*/
/*SELECT COUNTRY_ID FROM HR.COUNTRIES;*/

